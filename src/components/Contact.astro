---
import { Check, MessageCircle, Phone, Clock, Heart, Star } from "@lucide/astro"

// Lista de beneficios
const benefits = [
	"Asesoría personalizada",
	"Cotización inmediata",
	"Seguimiento de tu pedido",
	"Atención 24/7",
]
---

<section class="relative overflow-hidden" id="contacto">
	<!-- Fondo gradient elaborado -->
	<div
		class="absolute inset-0 bg-gradient-to-br from-pink-100 via-pink-200 to-pink-300"
	>
	</div>
	<div
		class="absolute inset-0 bg-gradient-to-tr from-transparent via-pink-300/30 to-pink-400/50"
	>
	</div>

	<!-- Elementos decorativos complejos -->
	<div
		class="absolute top-0 left-0 w-full h-full overflow-hidden pointer-events-none"
	>
		<!-- Círculos flotantes grandes -->
		<div
			class="absolute -top-20 -left-20 w-40 h-40 bg-pink-300/20 rounded-full animate-float-slow blur-2xl"
		>
		</div>
		<div
			class="absolute top-1/4 -right-16 w-32 h-32 bg-pink-400/25 rounded-full animate-float-reverse blur-xl"
		>
		</div>
		<div
			class="absolute bottom-10 left-10 w-24 h-24 bg-pink-200/30 rounded-full animate-bounce-gentle blur-lg"
		>
		</div>

		<!-- Formas geométricas -->
		<div
			class="absolute top-20 right-1/4 w-3 h-3 bg-pink-400/40 rounded-full animate-twinkle"
		>
		</div>
		<div
			class="absolute bottom-1/3 left-1/4 w-2 h-2 bg-pink-500/50 rounded-full animate-twinkle-delayed"
		>
		</div>
		<div
			class="absolute top-1/2 right-1/3 w-4 h-4 bg-pink-300/60 rounded-full animate-pulse-soft"
		>
		</div>

		<!-- Elementos de corazón -->
		<div class="absolute top-16 left-1/3 opacity-10 animate-float-heart">
			<Heart class="w-6 h-6 text-pink-400" fill="currentColor" />
		</div>
		<div
			class="absolute bottom-20 right-1/4 opacity-10 animate-float-heart-delayed"
		>
			<Star class="w-5 h-5 text-pink-500" fill="currentColor" />
		</div>
	</div>

	<div class="relative z-10 py-20 md:py-32">
		<div class="px-4 sm:px-8 md:px-16 lg:px-32 xl:px-72">
			<div
				class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-center"
			>
				<!-- Columna de texto (izquierda) - 7 columnas -->
				<div class="lg:col-span-7 space-y-8 lg:space-y-10">
					<!-- Encabezado principal -->
					<div class="space-y-6 animate-slide-in-left">
						<div
							class="inline-flex items-center gap-2 bg-pink-500/10 backdrop-blur-sm px-4 py-2 rounded-full border border-pink-300/30"
						>
							<MessageCircle class="w-4 h-4 text-pink-600" />
							<span class="text-sm font-medium text-pink-700"
								>Contacto directo</span
							>
						</div>

						<h2
							class="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-800 leading-tight"
						>
							¿Listo para hacer tu
							<span class="relative inline-block">
								<span class="relative z-10 text-pink-600">pedido?</span>
								<div
									class="absolute -bottom-2 left-0 w-full h-4 bg-pink-300/40 rounded-lg -rotate-1 animate-pulse-background"
								>
								</div>
							</span>
						</h2>

						<p
							class="text-xl md:text-2xl text-gray-700 leading-relaxed font-light"
						>
							Contáctanos a través de WhatsApp y te ayudaremos a crear el dulce
							perfecto para tu ocasión especial. Nuestro equipo está listo para
							asesorarte en cada paso del proceso.
						</p>
					</div>

					<!-- Lista de beneficios mejorada -->
					<div class="grid grid-cols-1 sm:grid-cols-2 gap-4 lg:gap-6">
						{
							benefits.map((benefit, index) => (
								<div
									class="flex items-center gap-4 bg-white/60 backdrop-blur-sm rounded-2xl p-4 lg:p-5 border border-pink-200/50 shadow-lg hover:shadow-xl transition-all duration-300 hover:bg-white/80 animate-slide-in-up group"
									style={`animation-delay: ${(index + 1) * 0.15}s`}
								>
									<div
										class="flex-shrink-0 w-10 h-10 lg:w-12 lg:h-12 bg-gradient-to-br from-pink-500 to-pink-600 rounded-full flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300 animate-scale-in"
										style={`animation-delay: ${(index + 1) * 0.2}s`}
									>
										<Check
											class="w-5 h-5 lg:w-6 lg:h-6 text-white"
											stroke-width="2.5"
										/>
									</div>
									<span class="text-lg lg:text-xl font-semibold text-gray-700 group-hover:text-gray-800 transition-colors duration-300">
										{benefit}
									</span>
								</div>
							))
						}
					</div>

					<!-- Información adicional -->
					<div
						class="flex flex-wrap gap-6 lg:gap-8 animate-fade-in-up"
						style="animation-delay: 1s"
					>
						<div class="flex items-center gap-3">
							<div
								class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center"
							>
								<Phone class="w-4 h-4 text-white" />
							</div>
							<span class="text-gray-700 font-medium">Respuesta inmediata</span>
						</div>
						<div class="flex items-center gap-3">
							<div
								class="w-8 h-8 bg-purple-500 rounded-full flex items-center justify-center"
							>
								<Clock class="w-4 h-4 text-white" />
							</div>
							<span class="text-gray-700 font-medium">Disponible 24/7</span>
						</div>
					</div>
				</div>

				<!-- Columna de WhatsApp (derecha) - 5 columnas -->
				<div
					class="lg:col-span-5 flex justify-center lg:justify-end animate-slide-in-right"
				>
					<div class="relative max-w-sm w-full">
						<!-- Card principal -->
						<div
							class="bg-white/90 backdrop-blur-lg rounded-3xl p-8 lg:p-10 shadow-2xl border border-pink-200/30 transform hover:scale-105 transition-all duration-500 animate-bounce-in relative overflow-hidden"
						>
							<!-- Elementos decorativos internos -->
							<div
								class="absolute top-4 right-4 w-16 h-16 bg-pink-100/50 rounded-full blur-xl"
							>
							</div>
							<div
								class="absolute bottom-4 left-4 w-12 h-12 bg-green-100/50 rounded-full blur-lg"
							>
							</div>

							<!-- Contenido principal -->
							<div class="relative z-10">
								<!-- Ícono de WhatsApp mejorado -->
								<div class="flex justify-center mb-8">
									<div class="relative">
										<div
											class="w-20 h-20 lg:w-24 lg:h-24 bg-gradient-to-br from-green-400 to-green-600 rounded-full flex items-center justify-center shadow-2xl animate-pulse-green"
										>
											<MessageCircle
												class="w-10 h-10 lg:w-12 lg:h-12 text-white"
												fill="currentColor"
											/>
										</div>
										<!-- Círculos concéntricos animados -->
										<div
											class="absolute inset-0 w-20 h-20 lg:w-24 lg:h-24 rounded-full border-2 border-green-300/30 animate-ping"
										>
										</div>
										<div
											class="absolute inset-2 w-16 h-16 lg:w-20 lg:h-20 rounded-full border border-green-200/40 animate-ping"
											style="animation-delay: 0.5s"
										>
										</div>
									</div>
								</div>

								<!-- Contenido -->
								<div class="text-center space-y-6">
									<div class="space-y-3">
										<h3
											class="text-2xl lg:text-3xl font-bold bg-gradient-to-r from-pink-500 to-pink-600 bg-clip-text text-transparent"
										>
											¡Haz tu pedido ahora!
										</h3>
										<p class="text-gray-600 text-lg font-medium">
											Escríbenos y te responderemos de inmediato
										</p>
									</div>

									<!-- Botón de WhatsApp mejorado -->
									<a
										href="https://wa.me/584126539866?text=¡Hola!%20Me%20interesa%20hacer%20un%20pedido%20en%20SugarBloom%20🍰"
										target="_blank"
										rel="noopener noreferrer"
										class="inline-flex items-center gap-4 bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-bold px-8 py-4 lg:px-10 lg:py-5 rounded-full transition-all duration-300 transform hover:scale-105 hover:shadow-2xl animate-pulse-slow group relative overflow-hidden"
									>
										<!-- Efecto de brillo -->
										<div
											class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-1000"
										>
										</div>

										<MessageCircle
											class="w-6 h-6 lg:w-7 lg:h-7 relative z-10"
										/>
										<span class="text-lg lg:text-xl relative z-10"
											>Chatear por WhatsApp</span
										>
									</a>

									<!-- Texto adicional -->
									<p class="text-sm text-gray-500 px-4">
										✨ ¡No te preocupes! Solo es una consulta, sin compromiso
									</p>
								</div>
							</div>
						</div>

						<!-- Elementos flotantes alrededor de la card -->
						<div
							class="absolute -top-4 -left-4 w-8 h-8 bg-pink-300/40 rounded-full animate-bounce-gentle blur-sm"
						>
						</div>
						<div
							class="absolute -bottom-6 -right-6 w-6 h-6 bg-green-300/40 rounded-full animate-bounce-gentle blur-sm"
							style="animation-delay: 1s"
						>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* Animaciones mejoradas y más complejas */
	@keyframes float-slow {
		0%,
		100% {
			transform: translateY(0px) rotate(0deg) scale(1);
		}
		50% {
			transform: translateY(-30px) rotate(180deg) scale(1.1);
		}
	}

	@keyframes float-reverse {
		0%,
		100% {
			transform: translateY(0px) rotate(0deg) scale(1);
		}
		50% {
			transform: translateY(20px) rotate(-180deg) scale(0.9);
		}
	}

	@keyframes bounce-gentle {
		0%,
		100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-15px);
		}
	}

	@keyframes twinkle {
		0%,
		100% {
			opacity: 0.3;
			transform: scale(1);
		}
		50% {
			opacity: 1;
			transform: scale(1.5);
		}
	}

	@keyframes twinkle-delayed {
		0%,
		100% {
			opacity: 0.2;
			transform: scale(1);
		}
		50% {
			opacity: 0.8;
			transform: scale(1.3);
		}
	}

	@keyframes pulse-soft {
		0%,
		100% {
			opacity: 0.4;
			transform: scale(1);
		}
		50% {
			opacity: 0.8;
			transform: scale(1.2);
		}
	}

	@keyframes float-heart {
		0%,
		100% {
			transform: translateY(0px) rotate(0deg);
			opacity: 0.1;
		}
		50% {
			transform: translateY(-20px) rotate(10deg);
			opacity: 0.3;
		}
	}

	@keyframes float-heart-delayed {
		0%,
		100% {
			transform: translateY(0px) rotate(0deg);
			opacity: 0.1;
		}
		50% {
			transform: translateY(-25px) rotate(-15deg);
			opacity: 0.25;
		}
	}

	@keyframes slide-in-left {
		0% {
			opacity: 0;
			transform: translateX(-80px) rotateY(-10deg);
		}
		100% {
			opacity: 1;
			transform: translateX(0) rotateY(0deg);
		}
	}

	@keyframes slide-in-right {
		0% {
			opacity: 0;
			transform: translateX(80px) rotateY(10deg);
		}
		100% {
			opacity: 1;
			transform: translateX(0) rotateY(0deg);
		}
	}

	@keyframes slide-in-up {
		0% {
			opacity: 0;
			transform: translateY(40px) scale(0.95);
		}
		100% {
			opacity: 1;
			transform: translateY(0) scale(1);
		}
	}

	@keyframes scale-in {
		0% {
			opacity: 0;
			transform: scale(0) rotate(-180deg);
		}
		70% {
			opacity: 1;
			transform: scale(1.1) rotate(0deg);
		}
		100% {
			opacity: 1;
			transform: scale(1) rotate(0deg);
		}
	}

	@keyframes bounce-in {
		0% {
			opacity: 0;
			transform: scale(0.3) translateY(100px) rotateZ(-10deg);
		}
		50% {
			opacity: 1;
			transform: scale(1.05) translateY(-20px) rotateZ(5deg);
		}
		70% {
			transform: scale(0.95) translateY(10px) rotateZ(-2deg);
		}
		100% {
			opacity: 1;
			transform: scale(1) translateY(0) rotateZ(0deg);
		}
	}

	@keyframes pulse-green {
		0%,
		100% {
			box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.7);
		}
		70% {
			box-shadow: 0 0 0 15px rgba(34, 197, 94, 0);
		}
	}

	@keyframes pulse-background {
		0%,
		100% {
			opacity: 0.4;
			transform: rotate(-1deg) scale(1);
		}
		50% {
			opacity: 0.7;
			transform: rotate(1deg) scale(1.05);
		}
	}

	@keyframes fade-in-up {
		0% {
			opacity: 0;
			transform: translateY(20px);
		}
		100% {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* Aplicación de animaciones */
	.animate-float-slow {
		animation: float-slow 8s ease-in-out infinite;
	}

	.animate-float-reverse {
		animation: float-reverse 10s ease-in-out infinite;
		animation-delay: 2s;
	}

	.animate-bounce-gentle {
		animation: bounce-gentle 3s ease-in-out infinite;
	}

	.animate-twinkle {
		animation: twinkle 2s ease-in-out infinite;
	}

	.animate-twinkle-delayed {
		animation: twinkle-delayed 2.5s ease-in-out infinite;
		animation-delay: 1s;
	}

	.animate-pulse-soft {
		animation: pulse-soft 3s ease-in-out infinite;
		animation-delay: 0.5s;
	}

	.animate-float-heart {
		animation: float-heart 6s ease-in-out infinite;
	}

	.animate-float-heart-delayed {
		animation: float-heart-delayed 7s ease-in-out infinite;
		animation-delay: 3s;
	}

	.animate-slide-in-left {
		animation: slide-in-left 1s ease-out forwards;
	}

	.animate-slide-in-right {
		animation: slide-in-right 1s ease-out forwards;
		animation-delay: 0.3s;
		opacity: 0;
	}

	.animate-slide-in-up {
		animation: slide-in-up 0.8s ease-out forwards;
		opacity: 0;
	}

	.animate-scale-in {
		animation: scale-in 0.7s ease-out forwards;
		opacity: 0;
	}

	.animate-bounce-in {
		animation: bounce-in 1.2s ease-out forwards;
		animation-delay: 0.6s;
		opacity: 0;
	}

	.animate-pulse-green {
		animation: pulse-green 2s infinite;
	}

	.animate-pulse-slow {
		animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
	}

	.animate-pulse-background {
		animation: pulse-background 4s ease-in-out infinite;
	}

	.animate-fade-in-up {
		animation: fade-in-up 0.8s ease-out forwards;
		opacity: 0;
	}

	/* Responsive adjustments */
	@media (max-width: 1024px) {
		.animate-slide-in-right {
			animation-delay: 0s;
		}

		.animate-bounce-in {
			animation-delay: 0.3s;
		}
	}

	/* Efectos adicionales */
	.group:hover .animate-pulse-green {
		animation-duration: 1s;
	}

	@media (prefers-reduced-motion: reduce) {
		* {
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
			transition-duration: 0.01ms !important;
		}
	}
</style>