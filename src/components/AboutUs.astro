<section class="py-16 md:py-24 bg-white">
	<div class="px-4 sm:px-8 md:px-16 lg:px-32 xl:px-72">
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-16 items-center">
			<!-- Columna de texto (izquierda) -->
			<div class="space-y-6 lg:space-y-8">
				<div class="space-y-4">
					<h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-pink-400">
						Sobre Nosotros
					</h2>
					<p class="text-xl md:text-2xl text-gray-700 font-medium">
						Nuestra pasión por la repostería artesanal
					</p>
				</div>

				<div class="space-y-6">
					<div>
						<h3 class="text-2xl md:text-3xl font-bold text-pink-400 mb-4">
							Una tradición familiar
						</h3>
						<p class="text-gray-600 text-lg leading-relaxed">
							En SugarBloom, cada dulce es creado con amor y dedicación. Desde
							2015, hemos estado endulzando momentos especiales con nuestras
							creaciones artesanales, utilizando solo los mejores ingredientes y
							técnicas tradicionales.
						</p>
					</div>

					<p class="text-gray-600 text-lg leading-relaxed">
						Nuestro equipo de reposteros expertos se especializa en crear
						experiencias únicas que despiertan todos los sentidos, desde el
						primer vistazo hasta el último bocado.
					</p>
				</div>

				<!-- Estadísticas -->
				<div class="grid grid-cols-3 gap-4 pt-8">
					<div class="text-center">
						<div
							class="text-3xl md:text-4xl lg:text-5xl font-bold text-pink-400 mb-2"
						>
							<span class="counter" data-target="1250">0</span>
						</div>
						<p class="text-sm md:text-base text-gray-600 font-medium">
							Clientes Felices
						</p>
					</div>
					<div class="text-center">
						<div
							class="text-3xl md:text-4xl lg:text-5xl font-bold text-pink-400 mb-2"
						>
							<span class="counter" data-target="9">0</span>
						</div>
						<p class="text-sm md:text-base text-gray-600 font-medium">
							Años de Experiencia
						</p>
					</div>
					<div class="text-center">
						<div
							class="text-3xl md:text-4xl lg:text-5xl font-bold text-pink-400 mb-2"
						>
							<span class="counter" data-target="150">0</span>
						</div>
						<p class="text-sm md:text-base text-gray-600 font-medium">
							Recetas Únicas
						</p>
					</div>
				</div>
			</div>

			<!-- Columna de imagen (derecha) -->
			<div class="relative">
				<div class="relative rounded-2xl overflow-hidden shadow-2xl">
					<img
						src="/artur.jpg"
						alt="Repostería artesanal SugarBloom"
						class="w-full h-auto object-cover"
					/>
					<!-- Overlay decorativo -->
					<div
						class="absolute inset-0 bg-gradient-to-tr from-pink-500/20 to-transparent"
					>
					</div>
				</div>

				<!-- Elemento decorativo -->
				<div
					class="absolute -bottom-6 -right-6 w-24 h-24 bg-pink-400 rounded-full opacity-20 blur-xl"
				>
				</div>
				<div
					class="absolute -top-6 -left-6 w-16 h-16 bg-pink-300 rounded-full opacity-30 blur-lg"
				>
				</div>
			</div>
		</div>
	</div>
</section>

<script>
	document.addEventListener("DOMContentLoaded", function () {
		// Función para animar los números
		function animateCounters() {
			const counters = document.querySelectorAll(".counter")

			counters.forEach((counter) => {
				const target = parseInt(counter.getAttribute("data-target") || "0")
				const duration = 2000 // 2 segundos
				const increment = target / (duration / 16) // 60 FPS
				let current = 0

				const updateCounter = () => {
					if (current < target) {
						current += increment
						counter.textContent = Math.floor(current).toString()
						requestAnimationFrame(updateCounter)
					} else {
						counter.textContent = target.toString()
					}
				}

				updateCounter()
			})
		}

		// Intersection Observer para activar la animación cuando sea visible
		const aboutSection = document.querySelector("section")
		if (aboutSection) {
			const observer = new IntersectionObserver(
				(entries) => {
					entries.forEach((entry) => {
						if (entry.isIntersecting) {
							animateCounters()
							observer.unobserve(entry.target)
						}
					})
				},
				{
					threshold: 0.5,
				}
			)

			observer.observe(aboutSection)
		}
	})
</script>
